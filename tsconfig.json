{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  // tools/**/*.ts is Node-only; browser runtime files are covered by tsconfig.browser.json
  "include": ["*.ts", "scripts/**/*.ts", "tests/**/*.ts", "tools/**/*.ts"],
  "exclude": ["node_modules", "vite.config.ts"]
}
